<!DOCTYPE html>
<html>
  <head>
    <title>UrbanSherpa</title>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag asset_path('favicon.png'), :rel => 'icon', :type =>  'image/png' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
  </head>

  <body>

    <div class = "container">
      <% if current_user %>
          <!-- <p class="nav-top"><%= current_user.email %> <span id="pipe">|</span> <%= link_to "Sign out", '/signout', id: "signout", style: ''%> | <%= link_to "Your Itinerary | #{tour_cart}", cart_path %> | <%= link_to "Home", tours_path %></p> -->
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <h1 class="navbar-brand">Urban Sherpa</h1>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link"> <%= link_to "Home", tours_path %></a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.user_name%></a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item">Profile</a>
                <a class="dropdown-item"><%= link_to "Itinerary | #{tour_cart}", cart_path %></a>
                <div class="dropdown-divider"></div>
                  <a class="dropdown-item">Sign Out</a>
              </div>
            </li>
          </ul>
        </div>
      </nav>
        <% else %>
          <p class="nav-bottom"><%= link_to "Sign up", '/signup', id:'sign_up'%> | <%= link_to "Sign in", '/signin', id:'sign_in'%> | <%= link_to "Home", tours_path %></p>
        <% end %>
      <%= flash[:alert] %>
      <%= flash[:notice] %>
      <br>
      <br>
    <%= yield %>
    </div>
  </body>

</html>
